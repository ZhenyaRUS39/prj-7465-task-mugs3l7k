# Plan: prj-7465-task-mugs3l7k

**Project:** `PRJ-4107`  
**Task ID:** `task-mugs3l7k`  
**Repo:** `prj-7465-task-mugs3l7k`  

## Summary

Декомпозиция задачи на утилиту подсчёта hash файлов: репо + CLI-скрипт с поддержкой MD5/SHA1/SHA256 + тесты + README. Последовательная цепочка из 4 subtask-ов.

## Subtasks

### 1. Инициализировать репозиторий и структуру

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать локальный репо prj-7465-task-mugs3l7k, привязать к origin https://github.com/ZhenyaRUS39/prj-7465-task-mugs3l7k.git, создать ветку feat/file-hash-utility, добавить .gitignore (Python), requirements.txt с pytest, README-заглушку и директорию tests/.
- **Test plan:** git remote -v указывает на ZhenyaRUS39/prj-7465-task-mugs3l7k; ветка feat/file-hash-utility существует; pytest установлен и собирает 0 тестов без ошибок.

### 2. Реализовать CLI-утилиту hash_files.py

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Скрипт на Python с argparse: поддержка алгоритмов md5/sha1/sha256 (через hashlib), чтение файлов чанками, вывод в формате <hash> <file>, обработка ошибок (файл не найден, нет прав), код возврата 0 при успехе, 1 при ошибках. Возможность передать несколько файлов или директорию через -d.
- **Test plan:** python hash_files.py file.txt выводит sha256-хеш; python hash_files.py -a md5 file.txt выводит md5; запуск на бинарном файле 10MB даёт совпадение с sha256sum; код возврата 1 при отсутствующем файле.
- **Dependencies:** `sub-1`

### 3. Написать тесты pytest

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Тесты в tests/test_hash_files.py: юнит-тесты на чистую функцию compute_hash(path, algorithm) — проверка для известных строк ('hello' → sha256 2cf24d...), корректность переключения алгоритма, обработка несуществующего файла (исключение), обработка бинарного файла. Покрытие ≥ 90% строк.
- **Test plan:** pytest -v проходит все тесты; pytest --cov=hash_files показывает ≥ 90% покрытие; тест на бинарный файл (1MB случайных байт) проходит за < 2 сек.
- **Dependencies:** `sub-2`

### 4. Документация и финальная проверка

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Заполнить README.md: установка, использование, примеры вывода, описание алгоритмов. Прогнать pytest -v, flake8 hash_files.py tests/. Вернуть diff Айтишнику для ревью — НЕ коммитить в main, НЕ пушить.
- **Test plan:** README содержит секции Installation/Usage/Examples; flake8 без ошибок; pytest -v зелёный; git diff origin/main..HEAD --stat показывает 4 файла (hash_files.py, tests/, README.md, requirements.txt).
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
